org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
# This builtInKotlin flag was added automatically by Flutter migrator
android.builtInKotlin=false
# This newDsl flag was added automatically by Flutter migrator
android.newDsl=false
org.gradle.java.home=C:\\Program Files\\Eclipse Adoptium\\jdk-17.0.19.10-hotspot
# Kotlin incremental compilation computes relative paths between plugin sources
# (in the Pub cache on C:) and the project (on D:). Cross-drive relative paths
# throw "different roots" on Windows and crash the release build, so disable it.
kotlin.incremental=false
